{
  "name": "encrypted-email-website",
  "version": "1.0.0",
  "description": "Personal website for sending PGP encrypted emails via Cloudflare Workers",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "openpgp": "^5.11.2"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^3.100.0"
  }
}
